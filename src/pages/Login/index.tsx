import { LoginForm } from '../../components/Forms/LoginForm';

export const Login = () => {
  return (
    <div className="bg-slate-800 max-w-[50rem] m-auto mt-24 p-8 flex justify-center items-center">
      <LoginForm />
    </div>
  );
};
